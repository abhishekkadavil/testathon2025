name: AI Performance Testing

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  ai-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install flask requests openai pytest

      - name: Generate new test cases (OpenRouter)
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: python tests/generate_tests.py || echo "⚠️ Skipping AI test generation due to rate limit"

      - name: Start Flask app in background
        run: |
          echo "🚀 Starting Flask app..."
          nohup python app.py > flask.log 2>&1 &
          sleep 5
          echo "✅ Flask server started on port 5000"
          curl -v http://127.0.0.1:5000/employee || echo "⚠️ Flask app not reachable yet"

      - name: Run functional tests
        run: pytest -q || true

      - name: Run performance tests
        run: python tests/perf_test.py

      - name: List generated files (debug)
        run: ls -R tests/generated

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: generated-tests-results
          path: |
            tests/generated/test_auto.py
            tests/generated/test_results.json

      - name: AI Performance Review
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: python tests/perf_review.py || echo "⚠️ Skipping AI performance review due to rate limit"
